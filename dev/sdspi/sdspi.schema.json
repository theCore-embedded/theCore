{
    "$schema": "http://json-schema.org/schema#",
    "type": "array",

    "items": {
        "type": "object",

        "properties": {
            "spi":       { "type": "string" },
            "cs_gpio":   { "type": "string" }
        },

        "required": [ "spi", "cs_gpio" ]
    }
}
