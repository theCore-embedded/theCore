{
    "$schema": "http://json-schema.org/schema#",
    "type": "array",

    "items": {
        "type": "object",

        "properties": {
            "id":        { "type": "string" },
            "spi":       { "type": "string" },
            "cs_gpio":   { "type": "string" }
        },

        "required": [ "id", "spi", "cs_gpio" ]
    }
}
