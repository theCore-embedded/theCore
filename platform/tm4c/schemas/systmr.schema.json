{
    "$schema": "http://json-schema.org/schema#",
    "type": "object",

    "properties": {
        "source":    { "enum": [ "systick" ] },
        "freq_hz":   { "type": "integer", "minimum": 20, "maximum": 1000 },
        "owner":     { "enum": [ "thecore", "user"] },
        "comment":   { "type": "string" }
    },

    "required": [ "source", "owner" ]
}
